# NVIDIA NIM (for OpenAI-compatible endpoint via LiteLLM)
NVIDIA_API_KEY=your_nvidia_api_key

# Mistral API (for provider `mistral_api`)
MISTRAL_API_KEY=your_mistral_api_key

# Gemini judge
GEMINI_API_KEY=your_gemini_api_key

# Amazon Bedrock (API key mode). Optional alternative: AWS_ACCESS_KEY_ID / AWS_SECRET_ACCESS_KEY / AWS_REGION_NAME.
# If any configured model uses `bedrock/...`, `boto3` must be installed (validated by --check-setup).
AWS_BEARER_TOKEN_BEDROCK=your_aws_bedrock_api_key

# Vercel AI Gateway API key
AI_GATEWAY_API_KEY=your_vercel_ai_gateway_api_key

# Optional convenience vars
# BENCHMARK_CONFIG=config.example.yaml
# Validate env/deps before a run:
# uv run python run.py --config config.example.yaml --check-setup

# LiteLLM (optional; adapter can also use provider-specific keys)
# LITELLM_API_KEY=your_litellm_or_gateway_api_key
